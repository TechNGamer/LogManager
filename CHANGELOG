v1.0.0 (Master):
- Main functionallity is present.
- LogTalker added.
- LogManager added.
- Creates folder in the dll's directory.

v1.0.1 (Master):
- Exposes log file locations.
- Exposes log folder location.

v1.0.2 (Master):
- Ensured that each line that LogManager gets ends with '\n'.

v1.0.3 (Master):
- Added LogTalker.FinishWriting().
	- Added the Obsolete Attribute to LogTalker.Close().
	- It is recommended to use LogTalker.FinishWriting() as it does not require creating a new object.

